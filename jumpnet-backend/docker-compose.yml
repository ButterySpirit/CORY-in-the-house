services:
  postgres:
    image: postgres:15
    container_name: jumpnet_postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: jumpnet_dev
    ports:
      - "5432:5432"
    networks:
      - jumpnet_network

  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jumpnet_backend
    restart: always
    ports:
      - "3000:3000"
    depends_on:
      - postgres
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USERNAME: admin
      DB_PASSWORD: password
      DB_DATABASE: jumpnet_dev
    networks:
      - jumpnet_network

networks:
  jumpnet_network:
